<%= form_with(model: @post, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <div class="message">
  <h3>以下の内容で、送信する。</h3>
    <%= @post.content %>
    <br><%= form.submit  "つぶやく" %>
    <%= form.submit "戻る", name: 'back' %></br>
  </div>
<% end %>
